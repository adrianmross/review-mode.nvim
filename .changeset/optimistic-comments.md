---
release: patch
---

Comments and replies now show in the thread panel and as a sign as soon as you post them, marked `sending…`, instead of after GitHub answers. If the post fails the comment is removed and your text is left in the unnamed register, so nothing you typed is lost.
