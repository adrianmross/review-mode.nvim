---
release: patch
---

Post PR comments, suggestions, thread replies and thread resolve/unresolve
without blocking the editor, and refetch review comments after a write instead
of serving the pre-write cache.
